// lib/sources/seekingAlpha.js
export async function fetchSeekingAlpha(symbols = []) {
  try {
    // Seeking Alpha - سيتم تفعيله لاحقاً
    return [];
  } catch (error) {
    console.error('❌ SeekingAlpha error:', error);
    return [];
  }
}
